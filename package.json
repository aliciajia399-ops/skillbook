{
  "name": "skillbook",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch": "node scripts/fetch-skills.js",
    "generate": "node scripts/generate-posters.js"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
